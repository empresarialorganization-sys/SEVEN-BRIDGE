name: one-shot-plugin-migration

on:
  push:
    branches: [main]

jobs:
  trigger:
    if: contains(github.event.head_commit.message, '[run-plugin-result3]')
    runs-on: ubuntu-latest
    steps:
      - name: Read SEVEN deployed keyboard migration result
        run: |
          curl --fail --silent --show-error \
            --retry 30 --retry-all-errors --retry-delay 3 \
            "https://seven-bridge.carlosdh12335.workers.dev/internal/one-shot/8f73d2c65aa74c46b5370fef95bcf89b?mode=result&id=9219c140-c9a6-44c8-a8f2-3c6e4d11aae1"
